[tool.ruff]
line-length = 110
target-version = "py310"
exclude = ["*/migrations/*"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "DJ"]

[tool.coverage.run]
omit = ["*/migrations/*", "manage.py", "config/wsgi.py"]
